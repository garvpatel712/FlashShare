# Example env file (safe to commit) - copy to `.env` and fill values

MONGODB_URI="mongodb+srv://username:password@cluster0.mongodb.net/flashshare?retryWrites=true&w=majority"
ADMIN_PASSWORD="replace_with_secure_password"
NEXT_PUBLIC_UPLOADCARE_KEY="demopublickey"
UPLOADCARE_SECRET_KEY="demosecretkey"
NEXT_PUBLIC_TTL_SECONDS=70
# Base URL used by Next.js metadata for Open Graph/Twitter images
# Set this to your production URL, e.g. https://flashshare.vercel.app
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
